import CategoryDashboard from "@/features/CategoryFeatures/CategoryDashboard";

const Categories = () => {
  return (
    <section className={`section bg-[#f6f8fd] h-full`}>
      <div className={`sectionContainer`}>
        <CategoryDashboard />
      </div>
    </section>
  );
};

export default Categories;
